# Your site-specific config — copy cluster.env.example to cluster.env and edit.
cluster.env

# Rendered per-node compose env (generated by render-env.sh; contains host IPs).
.env.dspark

# SSH keys / public keys — never commit.
*.key
*.pub
id_ed25519*
nvsync*

# Benchmark + run logs.
*.log
nccl-bench-*
/tmp/

# OS / editor cruft.
.DS_Store
*.swp
*~
